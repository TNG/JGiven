# Necessary to have the parallel tests actually run in parallel.
# Default settings will ensure that only the class annotated with a concurrent execution mode will
# be run in parallel.
junit.jupiter.execution.parallel.enabled = true
junit.jupiter.execution.parallel.mode.default = SAME_THREAD
junit.jupiter.execution.parallel.mode.classes.default = SAME_THREAD
